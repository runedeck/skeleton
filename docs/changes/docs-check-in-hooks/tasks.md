# Tasks

## 1. Implementation

- [x] 1.1 `rune-docs-check` hook in `.pre-commit-config.yaml` and `templates/base/.pre-commit-config.yaml`

## 2. Verification

- [x] 2.1 `prek run --all-files` on this repository with the hook present
- [ ] 2.2 `copier update` in deck and cli picks up the hook with the `CHANGELOG.md` trigger

## 3. Record

- [ ] 3.1 Archive moves `adr.md` to `docs/decisions/` with the next free SKEL number
