# Tasks

## 1. Template

- [x] 1.1 Remove `templates/base/CLAUDE.md`
- [x] 1.2 `templates/base/.gitignore` is the stack baseline, and the skeleton's own file carries it
- [x] 1.3 `missing_ignore_lines` in `scripts/consumer-parity.py` with its test

## 2. Consumers

- [ ] 2.1 cli, deck, docs, seer, bench, site, homebrew-tap, pi: `CLAUDE.md` and `GEMINI.md` deleted, baseline merged into `.gitignore`
- [ ] 2.2 cli `templates/skeleton/base/` mirrors the template

## 3. Record

- [ ] 3.1 Archive moves `adr.md` to `docs/decisions/` with the next free SKEL number
