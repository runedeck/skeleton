# Module purpose

This layer is intentionally thin: after `base/` lands, deck structure (module.yaml, rules/, skills/, agents/, defaults.yaml) is owned by the rune CLI.

```sh
rune init --module .
```
