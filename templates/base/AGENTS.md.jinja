# ${TITLE}

> Brief: ${BRIEF}

## Conventions

- `make install` activates the commit and push hooks; run it once after cloning.
- Commit format: `type: description` (feat, fix, docs, chore, refactor, test), lowercase, no scope.
- Every text file ends with a newline; 4-space indentation; no tabs outside Makefiles.
