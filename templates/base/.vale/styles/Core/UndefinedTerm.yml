# Generated by scripts/generate-vale-style.py from the STE rule source. Do not edit.
# Deslop (rule): a metaphor noun such as "gate" means nothing until the
# text says which check, file, or flag it stands for. A defined term is
# set in italics at first use, so an italic, bold, or backticked form
# anywhere in the text counts as the definition. The nouns are the
# TERMS subset of OVERUSE, so one list serves this rule and STE.Overused*.
# Raw scope, because Vale strips the markers before any other scope.
extends: conditional
message: "'%s' is never defined here. Set it in italics at first use and say what it is (rule Deslop)."
level: warning
ignorecase: true
scope: raw
first: '\b(arm|gate|knob|landscape|lane|surface)s?\b'
second: '[*_`](arm|gate|knob|landscape|lane|surface)s?[*_`]'
