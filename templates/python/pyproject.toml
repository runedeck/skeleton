[project]
name = "${NAME}"
version = "0.1.0"
# Literal string: a brief containing double quotes stays valid TOML.
description = '${BRIEF}'
license = "EUPL-1.2"
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
indent-width = 4

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "S"]
